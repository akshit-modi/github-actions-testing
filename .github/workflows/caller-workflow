name: caller workflow
on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  caller: 
    uses: akshit-modi/github-actions-testing/.github/workflows/reusable-workflow.yml@main
