name: CI
on:
  workflow_dispatch:

jobs:
  admin:
    runs-on: ubuntu-latest
    steps:
    - run: echo "admin"
  
  app:
    runs-on: ubuntu-latest
    steps:
    - run: echo "app"
    
  buy:
    runs-on: ubuntu-latest
    steps:
    - run: echo "buy"
    
  speed:
    runs-on: ubuntu-latest
    steps:
    - run: echo "speed"
